# Claude Code Optimizer - Calendar Integration Requirements
# Install with: pip install -r calendar_requirements.txt

# Google Calendar API
google-api-python-client==2.108.0
google-auth-httplib2==0.1.1
google-auth-oauthlib==1.2.0
google-auth==2.23.4

# iCal/ICS support
icalendar==5.0.11

# Timezone support
pytz==2023.3

# Data handling
python-dateutil==2.8.2

# Optional: Enhanced calendar features
caldav==1.3.6  # For CalDAV support (optional)
recurring-ical-events==2.1.1  # For recurring event processing (optional)

# Core dependencies (should already be available)
sqlite3  # Built into Python
json     # Built into Python
logging  # Built into Python
datetime # Built into Python
pathlib  # Built into Python
threading # Built into Python
uuid     # Built into Python
dataclasses # Built into Python (3.7+)