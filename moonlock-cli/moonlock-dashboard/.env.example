# Moonlock Dashboard Environment Variables
# Copy this file to .env.local for local development

# Optional: Custom Moonlock data directory (defaults to ~/.moonlock)
# MOONLOCK_DATA_DIR=/custom/path/to/moonlock

# Optional: API rate limiting (requests per minute)
# API_RATE_LIMIT=60

# Optional: Enable debug logging
# DEBUG=true